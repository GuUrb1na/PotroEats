<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MenuTheme" parent="Theme.AppCompat.Light">
        <item name="android:background">#ADD8E6</item>
        <item name="android:text">Menu</item>
    </style>

    <style name="TarjetaTheme" parent="Theme.AppCompat.Light">
        <item name="android:background">#ADD8E6</item>
        <item name="android:text">Tarjeta</item>
    </style>

</resources>
